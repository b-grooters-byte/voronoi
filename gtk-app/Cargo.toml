[package]
name = "voronoi-gtk"
version = "0.2.0"
edition = "2024"

[[bin]]
name = "voronoi-gtk"
path = "src/main.rs"

[dependencies]
voronoi-core = { path = "../core" }

gtk4 = "0.11.3"
glib = "0.22.7"
tokio = { version = "1", features = ["rt-multi-thread", "time", "sync"] }
